server:
    port: 40000
    tomcat:
        uri-encoding: utf-8
spring:
    datasource:
        url: jdbc:mysql://localhost/test?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&allowMultiQueries=true
        username: root
        password:
        driver-class: com.mysql.jdbc.Driver
    jpa:
        show-sql: true
        database: mysql
    redis:
        host: localhost
        password: 123456
        port: 6379
    jackson:
        time-zone: GMT+8
        date-format: yyyy-MM-dd HH:mm:ss
        joda-date-time-format: yyyy-MM-dd HH:mm:ss
logging:
    level:
        org.hibernate.SQL: DEBUG
        org.hibernate.type.descriptor.sql.BasicBinder: TRACE
        com.townmc: debug